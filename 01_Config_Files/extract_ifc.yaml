ifc_classes:  
  - IfcWindow
  - IfcDoor
  - IfcColumn
  - IfcBeam
  - IfcSlab
  - IfcRoof
  - IfcWall

data_expand: Ture
copies: 10

rotation:
  min: 0      # 最小旋转角度
  max: 360    # 最大旋转角度
translation:
  x_range: [-15, 15]  # x轴平移范围
  y_range: [-15, 15]  # y轴平移范围
axis: "z"     # 默认旋转轴
